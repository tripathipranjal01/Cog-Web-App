export interface IChartData {
  icon: string;
  heading: string;
  subHeading: string;
  chartData: Array<Array<unknown>>;
  totalValue: string;
  precentage: string;
  currentMonth: string;
  ytd: string;
}
